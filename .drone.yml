kind: pipeline
name: dev_build-arm

platform:
  os: linux
  arch: arm

steps:
  - name: push
    image: plugins/docker
    settings:
      repo: ramosisw/openvpn
      username: ramosisw
      tags: 
        - dev-arm
      password:
        from_secret: docker_password
