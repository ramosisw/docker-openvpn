kind: pipeline
name: dev_build-arm

platform:
  os: linux
  arch: arm

steps:
  - name: push
    image: plugins/docker
    settings:
      dockerfile: ./Dockerfile
      repo: ramosisw/openvpn
      username: ramosisw
      tags: 
        - ${DRONE_TAG}dev-arm
      password:
        from_secret: docker_password
